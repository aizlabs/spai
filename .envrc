# direnv configuration - automatically load environment
# Install direnv: https://direnv.net/

# Load .env file if it exists
dotenv_if_exists .env

# Use uv to manage virtual environment
layout python
